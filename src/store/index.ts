import { createStore } from 'vuex'
import auth from '@/store/modules/auth.module'
import galery from '@/store/modules/galery.module'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth,
    galery,
  },
})